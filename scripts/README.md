# Operational scripts

Download and preparation scripts belong here. They must require an explicit source URL or local source path, log data version/citation, and write only to ignored `ml/data/raw/` or `data/` paths. Do not automate download of gated data without the data provider's authorization.
